services:
  stage3api:
    build:
      context: stage3/
      dockerfile: Dockerfile
    container_name: uti-stage3-api
    command: uvicorn app:app --host 0.0.0.0 --port 7136 --reload
    ports:
      - "7136:7136"
    environment:
      MODELS_DIR: /app/models
    restart: unless-stopped