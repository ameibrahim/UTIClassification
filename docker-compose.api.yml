services:
  api:
    build:
      context: api/
      dockerfile: Dockerfile
    container_name: uti-api
    command: uvicorn app:app --host 0.0.0.0 --port 7135 --reload
    ports:
      - "7135:7135"
    environment:
      MODELS_DIR: /app/models
    restart: unless-stopped